apiVersion: v1
kind: ConfigMap
metadata:
  name: cicd-analyzer-backend
  namespace: cicd-analyzer
  labels:
    app.kubernetes.io/name: cicd-analyzer
    app.kubernetes.io/component: backend
data:
  OPENAI_MODEL: "gpt-4o"
  CORS_ORIGINS: "https://cicd-analyzer.example.com"
  LOG_LEVEL: "info"
  WORKERS: "2"
