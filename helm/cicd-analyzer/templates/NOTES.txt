Thank you for installing {{ .Chart.Name }}!

Your release is named: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

== Components ==

Backend:
  Service: {{ include "cicd-analyzer.fullname" . }}-backend:{{ .Values.backend.service.port }}
  Replicas: {{ .Values.backend.replicaCount }}
  {{- if .Values.backend.autoscaling.enabled }}
  Autoscaling: {{ .Values.backend.autoscaling.minReplicas }}-{{ .Values.backend.autoscaling.maxReplicas }} (CPU {{ .Values.backend.autoscaling.targetCPUUtilizationPercentage }}%)
  {{- end }}

Frontend:
  Service: {{ include "cicd-analyzer.fullname" . }}-frontend:{{ .Values.frontend.service.port }}
  Replicas: {{ .Values.frontend.replicaCount }}

{{- if .Values.ingress.enabled }}

== Ingress ==

  URL: https://{{ .Values.ingress.host }}
  TLS: {{ .Values.ingress.tls.enabled }}
  Paths:
    /     -> frontend
    /api  -> backend
{{- end }}

== Getting Started ==

1. Check pod status:
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

2. Check services:
   kubectl get svc -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

{{- if not .Values.ingress.enabled }}

3. Port-forward to access locally:
   kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "cicd-analyzer.fullname" . }}-frontend 8080:{{ .Values.frontend.service.port }}
   kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "cicd-analyzer.fullname" . }}-backend 8000:{{ .Values.backend.service.port }}
{{- end }}

== Important ==

Make sure to update the secret values before deploying to production.
Consider using external-secrets-operator or sealed-secrets for secret management.
